const blogValidationSchema = require('./blogValidation');
const userRegisterValidatonSchema = require('./userRegisterValidation');
const loginValidationSchema = require('./loginValidation');

module.exports = {
    blogValidationSchema,
    userRegisterValidatonSchema,
    loginValidationSchema,
}